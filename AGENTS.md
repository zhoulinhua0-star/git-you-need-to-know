# AGENTS.md — `git-you-need-to-know`

## 项目使命

构建 `git-you-need-to-know`：一本极简、实用的 Git 手册，读起来像一本精炼的电子技术书。

读者打开任何一页，都应该能快速理解核心概念，并带着一个正确、可以立即应用的心智模型离开。

整体风格为**极简技术 / 极客风**：

- 高信息密度，低噪声。
- 快速吸收知识。
- 层级清晰，留白充足。
- 技术准确，不增加无谓复杂度。
- 适合快速浏览、系统学习、收藏和反复查阅。

仓库本身也应该成为学习 Git 的实践环境。合适时，使用本项目自己的 commits、branches、Issues、Pull Requests 和贡献流程，作为所讲概念的真实示例。

## 发布模式

### GitHub：内容源头

GitHub 仓库是手册规范、完整且持续维护的版本。

- 推荐仓库名：`git-you-need-to-know`
- 在 GitHub 保存完整章节和最新修正。
- 通过 Issues 和 Pull Requests 接受改进与修正。
- 把 GitHub 历史与协作过程也视为学习体验的一部分。

### 小红书：精简传播版本

小红书是发现与传播渠道，不是规范内容的存储位置。

- 第一篇内容可以命名为**《有关 Git，你需要知道的一切》**。
- 只发布能让读者立即获得实用 Git 全局地图的核心概念。
- 针对移动端快速阅读重新编排，而不是逐字复制 GitHub 内容。
- 引导读者前往 GitHub 获取完整且持续更新的手册。
- 邀请读者通过 GitHub 提交修正、问题、Issues 和 Pull Requests。

简而言之：

```text
小红书
精简入口 + 内容发现
          ↓
GitHub
完整手册 + 持续更新 + 开放协作
```

## 必需的仓库结构

使用以下结构：

```text
git-you-need-to-know/
├── README.md
├── docs/
│   ├── 01-mental-model.md
│   ├── 02-repository.md
│   ├── 03-staging-area.md
│   ├── 04-commit.md
│   ├── 05-branch.md
│   ├── 06-remote.md
│   ├── 07-merge.md
│   ├── 08-conflict.md
│   ├── 09-rebase.md
│   ├── 10-undo.md
│   ├── 11-github-workflow.md
│   └── 12-advanced.md
├── cheatsheet.md
├── git-command-options.md
├── LICENSE
└── CONTRIBUTING.md
```

不要把整本手册都放进 `README.md`。

`README.md` 是封面、导读与目录。它应简要介绍项目、说明目标读者、展示学习路径、链接全部章节，并指向速查表和贡献指南。

## 核心章节

手册必须按以下顺序包含这些章节：

1. **心智模型** — 先解释 Git 跟踪什么，并介绍完整流程，再教授命令。
2. **仓库** — 解释本地仓库、历史、实用层面的对象模型、初始化和 clone。
3. **工作区与暂存区** — 解释 working tree、index、status，以及如何为下一次 commit 选择改动。
4. **提交（Commit）** — 解释快照、commit 身份、有效的提交消息和历史检查。
5. **分支（Branch）** — 解释 branch 是可移动引用、`HEAD`、切换 branch，以及轻量的并行工作。
6. **远程仓库（Remote）** — 解释远程仓库、remote-tracking branches、fetch、pull 和 push。
7. **合并（Merge）** — 解释 fast-forward 与 three-way merge、何时使用 merge，以及历史如何变化。
8. **冲突（Conflict）** — 解释冲突原因，并提供冷静、可重复的解决流程。
9. **变基（Rebase）** — 解释重放 commits、常见用途、取舍，以及不得随意重写共享历史的原则。
10. **撤销与恢复** — 比较 restore、revert、reset 和 reflog；明确标记破坏性或历史重写操作。
11. **GitHub 协作流程** — 把 fork、clone、branch、commit、push、Pull Request、review 和 merge 解释成一个完整流程。
12. **Git 进阶** — 精选 stash、cherry-pick、tags、bisect、worktree、hooks 和实用内部机制，避免变成命令堆砌。

文件名可以保持简洁，但章节标题与导航应使用上面完整的读者名称，尤其是**工作区与暂存区**和 **Git 进阶**。

## 编辑原则

每一页都必须遵循五项原则。

### 1. 极简

- 只包含建立理解和完成常见任务所必需的内容。
- 删除重复、填充文字、无意义的鼓励和装饰性复杂度。
- 一段准确解释优于多段近似解释。

### 2. 可视化

- 当关系能因此更清楚时，优先使用小型图示、状态流、对比表和带注释的命令序列。
- 视觉内容必须能在 GitHub Markdown 和窄屏上清楚阅读。
- 只有在有助于理解模型时才使用文本图或 Mermaid，不用它们装饰页面。

### 3. 实用

- 把每个概念连接到真实操作或场景。
- 使用可运行的命令和小而连贯的示例。
- 合适时展示预期结果、常见错误和安全恢复步骤。
- 区分安全操作、破坏性操作和历史重写操作。

### 4. 心智模型优先

- 先解释 Git 正在做什么，再展示命令。
- 把命令作为对模型的操作来介绍，而不是孤立语法。
- 在整本书中持续呈现主流程：

```text
工作区 → 暂存区 → 本地仓库 → 远程仓库
 编辑      暂存       提交        推送
```

### 5. 开放

- 把手册视为持续演进的开源项目。
- 让修正和贡献路径显而易见。
- 欢迎 Issues 和 Pull Requests。
- 优先选择透明、可维护的决定，而不是只有作者理解的约定。

## 写作风格

为阅读速度、清晰度与记忆效果而写。

- 使用短句和短段落。
- 保持充足留白。
- 开头先讲核心概念。
- 当代码块、紧凑图示和对比表比文字更快时，优先使用它们。
- 每次只介绍一个概念。
- 重要术语首次出现时给出定义。
- 所有章节使用一致术语。
- 命令示例保持聚焦，避免密集命令墙。
- 解释每个重要命令的效果，而不只是语法。
- 只在警告、规则和高价值洞见中少量使用 callout。
- 链接前置章节和下一步章节。
- GitHub 版本避免长篇个人叙事、日记式开头和泛泛介绍。
- 不要为简短而牺牲技术准确性。使用简化解释时，应标明适用边界。

介绍一个概念时，优先采用以下结构：

```text
概念
→ 可视化模型
→ 最小示例
→ 什么发生了变化
→ 常见错误或安全提示
→ 下一个相关概念
```

## README 要求

README 应保持简洁，并包含：

- 项目标题和一句话承诺。
- 手册用途的简短说明。
- 目标读者。
- Git 的四位置心智模型。
- 全部 12 章的链接目录。
- 指向 `cheatsheet.md` 的链接。
- 指向 `git-command-options.md` 的链接。
- 简短说明本仓库本身也是 Git 实践场所。
- 链接到 `CONTRIBUTING.md` 的贡献邀请。
- 说明手册会持续更新。

首屏应让项目价值和阅读路径一目了然，不要求读者长距离滚动。

## 支持文件

### `cheatsheet.md`

创建便于浏览的参考页，按使用意图而不是命令字母顺序组织。例如：检查、创建、暂存、提交、branch、同步、整合、撤销和恢复。

速查表用于补充章节，不能替代概念解释，也不能成为未经筛选的命令清单。

### `git-command-options.md`

解释 Git 命令行的全局选项、子命令选项、参数、短选项、长选项和 `--` 分隔符。强调选项含义由完整命令和作用域决定，并列出常用选项、危险操作及官方帮助查询方式。

这份资料应帮助读者学会解析命令，而不是鼓励脱离上下文背诵字母。

### `CONTRIBUTING.md`

解释如何：

- 报告错误或不清楚的说明。
- 提议内容改进。
- 创建 branch 并提交 Pull Request。
- 遵循项目写作和格式约定。
- 提交前验证链接、命令、图示和 Markdown。

贡献路径应对第一次使用 Git 和参与开源的贡献者友好。

## 小红书改编规则

创建小红书版本时：

- 保留核心心智模型和最有用的示例。
- 使用比 GitHub 版本更强的开头和稍微更口语化的表达。
- 小节保持简短，适合移动端阅读。
- 不要试图在一篇内容中复刻全部 12 章。
- 结尾明确引导读者前往 GitHub 获取完整版。
- 说明完整手册会持续演进。
- 欢迎通过 GitHub Issues 和 Pull Requests 提交修正与参与协作。

小红书内容是精心编排的入口，GitHub 仓库仍是完整参考版本。

## 后续 Agent 的执行优先级

继续这个项目时：

1. 保留本文件中的使命、发布模式、必需结构和章节顺序。
2. 先建立读者的心智模型，再扩展命令覆盖范围。
3. 把 README 建成导航和导读，而不是整本书。
4. 除非用户明确指定其他章节，否则按顺序开发。
5. 各章示例保持内在一致，最好使用同一个小型示例仓库。
6. 发布前验证命令与技术结论。
7. 检查全部相对链接和章节导航。
8. GitHub 与小红书内容应相互关联，但需要针对不同阅读场景有意改编。
9. 修改应保持聚焦；没有明确需要时，不引入无关工具或沉重的文档框架。
10. 对尚未确定的语言、品牌、托管或文档站点生成器作出重大选择前，先询问用户。

## 完成标准

一页内容达到以下标准时，才算完成：

- 开头几行就能看懂本页目的。
- 心智模型准确且容易记忆。
- 示例最小、连贯且技术上正确。
- 危险命令有明确警告，并在适当时提供更安全的替代方案。
- 在桌面端和移动端都容易浏览。
- 导航链接正常。
- 内容符合五项编辑原则。
- 读者阅读后能解释该概念，并在真实工作流中使用它。

## 后续系列

项目命名、语气和结构应允许未来扩展为一致的极简技术手册系列：

- `docker-you-need-to-know`
- `linux-you-need-to-know`
- `networking-you-need-to-know`

除非用户要求，否则现在不要构建这些项目。只在不会增加无谓复杂度时，让当前决定可以复用。
